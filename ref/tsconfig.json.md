# tsconfig.json.md

## 개요
Vite/React용 TypeScript 컴파일 설정입니다.

## 하이라이트
- target/lib ES2022, DOM/Iterable.
- module ESNext, resolution bundler, allowJs, JSX react-jsx, noEmit.
- paths alias `@/*` → `./*`, `@assets/*` → `./assets/*`.
- skipLibCheck, isolatedModules, allowImportingTsExtensions.
