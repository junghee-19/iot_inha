# index.html.md

## 개요
Vite가 서빙하는 기본 HTML 템플릿으로, 전역 스타일과 import map을 설정합니다.

## 주요 포인트
- Tailwind CDN, Noto Sans KR 폰트, 커스텀 body 스타일(상하단 다크 밴드, 페이드인 애니메이션, 스크롤바 숨김)을 로드합니다.
- React/ReactDOM CDN을 가리키는 import map을 설정합니다.
- 마운트 노드 `<div id="root"></div>`에 `/index.tsx` 모듈을 로드합니다.
